% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mle_grid_search.R
\name{mle_grid_search}
\alias{mle_grid_search}
\title{MLE via grid search}
\usage{
mle_grid_search(loglike, lower, upper, grid_size, refine_solver = NULL, ...)
}
\arguments{
\item{loglike}{Log-likelihood function}

\item{lower}{Lower bounds for parameters (numeric vector)}

\item{upper}{Upper bounds for parameters (numeric vector)}

\item{grid_size}{Grid resolution. Either a single integer (same resolution
per dimension) or a vector of integers (one per dimension).}

\item{refine_solver}{Optional local solver to refine each grid point.
If NULL, only evaluates loglike at grid points without refinement.}

\item{...}{Additional arguments passed to refine_solver}
}
\value{
mle object with best solution found, including:
  \item{theta.hat}{Best parameter estimate}
  \item{loglike}{Log-likelihood at best point}
  \item{grid_size}{Grid resolution used}
  \item{n_evaluated}{Number of grid points evaluated}
}
\description{
Performs exhaustive grid search over a bounded parameter space.
Optionally refines each grid point using a local solver.
}
\examples{
\dontrun{
# Simple grid search without refinement
loglike <- function(theta) {
  -(theta[1]^2 + theta[2]^2)
}

result <- mle_grid_search(
  loglike = loglike,
  lower = c(-5, -5),
  upper = c(5, 5),
  grid_size = 20
)

# Grid search with local refinement
score <- function(theta) {
  -2 * theta
}

result <- mle_grid_search(
  loglike = loglike,
  lower = c(-5, -5),
  upper = c(5, 5),
  grid_size = 10,
  refine_solver = mle_gradient_ascent,
  score = score,
  config = mle_config_gradient(eta = 0.1, max_iter = 20)
)

# Different resolution per dimension
result <- mle_grid_search(
  loglike = loglike,
  lower = c(-5, -5),
  upper = c(5, 5),
  grid_size = c(20, 10)  # 20 points in dim 1, 10 in dim 2
)
}
}
