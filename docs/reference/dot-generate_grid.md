# Generate grid of parameter values

Generate grid of parameter values

## Usage

``` r
.generate_grid(lower, upper, grid_size)
```

## Arguments

- lower:

  Lower bounds

- upper:

  Upper bounds

- grid_size:

  Grid resolution per dimension

## Value

Matrix where each row is a parameter vector
