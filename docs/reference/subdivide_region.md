# subdivide_region

subdivide_region

## Usage

``` r
subdivide_region(lower, upper, grid_size)
```

## Arguments

- lower:

  lower bounds of parameter support (vector)

- upper:

  upper bounds of parameter support (vector)

- grid_size:

  maximum size of each dimension of each hypercube that makes up the
  grid
