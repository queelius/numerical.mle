# mle_solve

Uses various functions and algorithms to find the MLE.

## Usage

``` r
mle_solve(options, theta0, method = c("optim"), ...)
```

## Arguments

- theta0:

  numeric vector, initial guess for MLE
